- name: Run full test suite
  run: pytest --junitxml=test-results.xml --maxfail=0 --disable-warnings -q

- name: Show test summary
  if: always()
  uses: pmeier/pytest-results-action@main
  with:
    path: test-results.xml
    fail-action: false  # ne bukjon el summary-tól
